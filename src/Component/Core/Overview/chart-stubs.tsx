// Temporary stub components until react-chartjs-2 and chart.js are installed
// Replace these imports with actual chart components after running: npm install

import { Component } from 'react';

export class Line extends Component<any> {
  render() {
    return <div className="h-full flex items-center justify-center text-gray-400">loading...</div>;
  }
}

export class Bar extends Component<any> {
  render() {
    return <div className="h-full flex items-center justify-center text-gray-400">loading...</div>;
  }
}
